'use strict';

// modules

const utils = require('./utils');

// private variables

const MILLISECONDS_PER_SECOND = 1000;

// exports

const seconds = utils.multiply(MILLISECONDS_PER_SECOND);

module.exports = seconds;
